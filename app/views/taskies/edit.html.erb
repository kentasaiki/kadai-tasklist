<h1>id: <%= @tasky.id %> のタスク編集ページ</h1>

<%= form_for(@tasky) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', taskies_path %>
